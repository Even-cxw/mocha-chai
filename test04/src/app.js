function add(a, b) {
  return a + b;
}
module.exports = {
  add,
}

document.scrollingElement.scrollTop = 0;
window.scrollTo(0,0);

document.scrollingElement.scrollTop = 0;
window.scrollTo(0,0)
document.addEventListener('focusout', ()=> {})
document.addEventListener('focusout', ()=> {})
